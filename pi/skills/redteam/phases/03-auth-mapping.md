# Phase 3: Authenticated Surface Mapping

Requires at least one provisioned test account and available env secret. Capture sessions through approved tooling and store only env references/redacted tokens. Re-run content discovery with auth, find Swagger/OpenAPI/GraphQL specs, enumerate methods and parameters, and identify resource identifiers for Phase 5. JWTs queue safe algorithm/kid/audience checks. GraphQL introspection returning schema is a finding only if not intended public documentation.
