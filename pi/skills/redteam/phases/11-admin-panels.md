# Phase 11: Admin Panels and Internal-by-name

Probe only admin-like hosts already discovered and approved as in-scope. Do not fabricate `admin.example.com` unless wildcard scope plus discovery/approval permits it. Login page exposure is informational/low unless bypass/default creds/user enumeration exists and is RoE-approved. Header-based IP bypasses are high priority.
